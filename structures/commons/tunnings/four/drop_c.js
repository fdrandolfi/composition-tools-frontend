import { getNoteDataById } from '../../notes';

// Drop C = [9, 4, 11, 16]

const drop_c = [
  getNoteDataById(21), // F2
  getNoteDataById(16), // C2
  getNoteDataById(11), // G1
  getNoteDataById(4),  // C1
];

export default drop_c;
