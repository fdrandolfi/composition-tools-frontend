import { getNoteDataById } from '../../notes';

// Drop A = [3, 10, 6, 1, 8, 1]

const drop_a = [
  getNoteDataById(39), // B3
  getNoteDataById(34), // F#3
  getNoteDataById(30), // D3
  getNoteDataById(25), // A2
  getNoteDataById(20), // E2
  getNoteDataById(13), // A1
];

export default drop_a;
