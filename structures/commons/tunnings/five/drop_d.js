import { getNoteDataById } from '../../notes';

// Drop D

const drop_d = [
  getNoteDataById(23), // G2
  getNoteDataById(18), // D2
  getNoteDataById(13), // A1
  getNoteDataById(6),  // D1
  getNoteDataById(3),  // B0
];

export default drop_d;
